<div id="<%= dom_id category %>">
  <p>
    <strong>User:</strong>
    <%= category.user.name %>
  </p>

  <p>
    <strong>Name:</strong>
    <%= category.name %>
    <small>Total Amount: <%= category.total_expense_amount %></small>
  </p>

  <p>
    <strong>Icon:</strong>
    <%= category.icon %>
  </p>

</div>
