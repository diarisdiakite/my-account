<%= render 'shared/header_forms' %>

<%= render "form_edit", category: @category %>

<br>

<div>
  <%= link_to "Show this category", user_category_path(@user, @category) %> |
  <%= link_to "Back to categories", user_categories_path(@user) %>
</div>
